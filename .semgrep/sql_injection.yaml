rules:
  - id: python-sql-injection-demo
    pattern: conn.execute("..." + $X)
    message: "Possible SQL Injection: unsafe query building with string concatenation"
    severity: ERROR
    languages: [python]
